# Used only by Vite's local development proxy.
VITE_API_PROXY_TARGET=http://localhost:4000

# Required when publishing the frontend separately from the API.
VITE_API_URL=https://your-api.onrender.com/api
